# Estimate — WP Content Autopilot

**Prepared by:** Shakil Ahmed · BarakahSoft LLC
**Rate:** $150/hr
**Date:** 2026-09-02

---

## Phase 0 — Working demo · **already built, not billed**

Delivered before any agreement, as proof this works end to end:

- Article generation against a configurable system prompt (your trained
  ChatGPT instructions paste straight into Settings), Gemini + OpenAI +
  a deterministic simulator fallback
- Full formatting pipeline: dash stripping, HTML stripping, heading wrapping
  in your CSS class, CTA shortcode insertion at a configurable paragraph,
  keyword→URL auto-linking from an editable dictionary
- Topic image generation (title baked onto a branded template, 3 template
  styles to start) — instant, no Photoshop step
- ACF field-mapping settings (title/body/CTA/image1/image2 → your field
  names) with a live preview of the exact payload before it posts
- WordPress publish via REST + ACF REST, instant or scheduled, with a
  simulated fallback when no site is connected yet so the whole flow can be
  reviewed with zero setup
- `/api/format-rules` and `/api/health` — audit endpoints, not "trust me"

**≈9 hours. Billed at $0.**

---

## What's next

| # | Phase | Scope | Hrs | @ $150 |
|---|---|---:|---:|---:|
| 1 | Wire to your real site | Connect your WP URL + application password, confirm your ACF/REST config, map your real field names | 2 | $300 |
| 2 | Match your exact templates | Recreate your 2-3 `.psd` layouts pixel-for-pixel instead of the 3 built-in styles | 3 | $450 |
| 3 | Batch mode | Paste a list of titles, generate + publish/schedule all of them in one run | 3 | $450 |

Phase 1 alone gets you live: real posts, real fields, today.

## The number that matters

Full scope above (8 hrs) at $150/hr is $1,200. This is quoted as a
**$100 fixed price, 2 days**, because Phase 0 is already built, already
running, and already worth more than the price — the remaining work is
mostly plugging in your real credentials and templates, not new
architecture.

## What I need from you

1. Your WordPress site URL + an Application Password (Users → Profile →
   Application Passwords)
2. Your real ACF field names for the 3-4 fields you currently paste into
3. Your keyword→link dictionary and CTA shortcode text
4. Your 2-3 `.psd` templates, if you want Phase 2
5. The system prompt from your ChatGPT Project
